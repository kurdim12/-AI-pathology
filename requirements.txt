# Naseej dependencies. Built on PyTorch + torchvision.
# For a CPU-only install, prefer the PyTorch CPU wheels:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.23.0
pillow>=9.0.0
scikit-learn>=1.1.0
matplotlib>=3.5.0
gradio>=4.0.0
tqdm>=4.64.0

# Optional:
#   pytest>=7.0.0    # to run the test suite (tests/)
#   timm>=0.9.0      # foundation-model upgrade path (src/model.build_foundation_model)
